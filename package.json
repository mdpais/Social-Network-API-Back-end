{
  "name": "social-network-api",
  "version": "1.0.0",
  "description": "Social Network Back-End where users can share their thoughts, react to friends' thoughgts and create a friend list.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server",
    "seed": "node utils/seed",
    "dev": "nodemon server"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^6.0.13",
    "nodemon": "^2.0.20"
  },
  "devDependencies": {
    "nodemon": "^2.0.9"
  }
}
